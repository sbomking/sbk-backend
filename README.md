# sbk-backend
SBOM management

In the UI, the user can choose multiple dependency scan with a priority.


SBOM management, the SBOM is stored in DB for the deployed versions.
  Afterwards, it is stored in S3, if the option is enabled otherwise it is destroyed.
